# Bulgarian Subtitles Addon for Stremio

🎬 Stremio addon за български субтитри от **Subsunacs.net** и **Subs.sab.bz**

## Функционалности

- ✅ Търсене на български субтитри за филми
- ✅ Търсене на български субтитри за сериали (с епизод)
- ✅ Поддръжка на два източника: Subsunacs.net и Subs.sab.bz
- ✅ Автоматично разпознаване по IMDB ID

## Инсталация

### 1. Инсталирай зависимостите

```bash
npm install
```

### 2. Стартирай addon-а

```bash
npm start
```

Или за development с auto-reload:

```bash
npm run dev
```

### 3. Добави в Stremio

Отвори Stremio и добави addon-а чрез URL:

```
http://localhost:7000/manifest.json
```

## Конфигурация

По подразбиране addon-ът работи на порт **7000**. Можеш да го промениш чрез environment variable:

```bash
PORT=8080 npm start
```

## Структура на проекта

```
├── index.js              # Главен файл на addon-а
├── providers/
│   ├── subsunacs.js      # Provider за Subsunacs.net
│   └── subssab.js        # Provider за Subs.sab.bz
├── package.json
└── README.md
```

## Деплойване

### На Beamup (препоръчително)

```bash
npx beamup
```

### На Heroku

1. Създай Heroku app
2. Push към Heroku:
```bash
git push heroku main
```

### С Docker

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --production
COPY . .
EXPOSE 7000
CMD ["npm", "start"]
```

## Източници на субтитри

| Източник | URL | Описание |
|----------|-----|----------|
| Subsunacs.net | https://subsunacs.net | Един от най-големите български сайтове за субтитри |
| Subs.sab.bz | http://subs.sab.bz | Популярен български сайт за субтитри |

## Бележки

- Addon-ът използва IMDB ID за идентификация на филми/сериали
- Субтитрите се търсят паралелно в двата източника
- Кодирането на българските сайтове е windows-1251

## Лиценз

MIT
